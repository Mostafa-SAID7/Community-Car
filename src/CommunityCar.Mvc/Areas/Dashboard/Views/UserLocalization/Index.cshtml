@{
    ViewData["Title"] = "User Localization";
}

<div class="container-fluid py-4">
    <partial name="_LocalizationNav" />
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-0 text-gray-800">User Localization</h1>
            <p class="text-muted small">Manage translations for user-facing profile fields, notifications, and identity messages.</p>
        </div>
    </div>

    <div class="card border-0 shadow-sm rounded-3">
        <div class="card-body p-5 text-center">
             <div class="mb-4">
                <i class="fas fa-user-edit fa-3x text-primary opacity-25"></i>
            </div>
            <h5 class="fw-bold">User Translation Center</h5>
            <p class="text-muted">Localization resources for user profiles and accounts will be managed here.</p>
        </div>
    </div>
</div>
