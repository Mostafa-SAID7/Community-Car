@{
    ViewData["Title"] = "User Reports";
}

<div class="container-fluid py-4">
    <partial name="_ReportsNav" />
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-0 text-gray-800">User Reports</h1>
            <p class="text-muted small">View reports related to user activity, moderation, and flags.</p>
        </div>
    </div>

    <div class="card border-0 shadow-sm rounded-3">
        <div class="card-body p-5 text-center">
             <div class="mb-4">
                <i class="fas fa-user-tag fa-3x text-primary opacity-25"></i>
            </div>
            <h5 class="fw-bold">User Reports Dashboard</h5>
            <p class="text-muted">User-specific reports and data exports will be available here.</p>
        </div>
    </div>
</div>
