@{
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    var isUsers = currentController?.Equals("UserLocalization", StringComparison.OrdinalIgnoreCase) == true;
    var isContent = currentController?.Equals("ContentLocalization", StringComparison.OrdinalIgnoreCase) == true;
}

<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link @(isUsers ? "active fw-bold border-bottom-0" : "text-secondary")" asp-area="Dashboard" asp-controller="UserLocalization" asp-action="Index">
            <i class="fas fa-user-edit me-2"></i>Users
        </a>
    </li>
    <li class="nav-item">
        <a class="nav-link @(isContent ? "active fw-bold border-bottom-0" : "text-secondary")" asp-area="Dashboard" asp-controller="ContentLocalization" asp-action="Index">
            <i class="fas fa-language me-2"></i>Content
        </a>
    </li>
</ul>
