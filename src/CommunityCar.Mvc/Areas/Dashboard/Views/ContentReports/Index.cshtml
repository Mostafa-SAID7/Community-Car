@{
    ViewData["Title"] = "Content Reports";
}

<div class="container-fluid py-4">
    <partial name="_ReportsNav" />
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-0 text-gray-800">Content Reports</h1>
            <p class="text-muted small">View reports related to content quality, flags, and reporting trends.</p>
        </div>
    </div>

    <div class="card border-0 shadow-sm rounded-3">
        <div class="card-body p-5 text-center">
             <div class="mb-4">
                <i class="fas fa-flag fa-3x text-primary opacity-25"></i>
            </div>
            <h5 class="fw-bold">Content Reports Dashboard</h5>
            <p class="text-muted">Content performance reports and moderation logs will be available here.</p>
        </div>
    </div>
</div>
