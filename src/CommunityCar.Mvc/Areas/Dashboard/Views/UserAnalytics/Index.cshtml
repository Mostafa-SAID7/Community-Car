@{
    ViewData["Title"] = "User Analytics";
}

<div class="container-fluid py-4">
    <partial name="_AnalyticsNav" />
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-0 text-gray-800">User Analytics</h1>
            <p class="text-muted small">Analyze user growth, engagement, and activity.</p>
        </div>
    </div>

    <div class="card border-0 shadow-sm rounded-3">
        <div class="card-body p-5 text-center">
             <div class="mb-4">
                <i class="fas fa-chart-line fa-3x text-primary opacity-25"></i>
            </div>
            <h5 class="fw-bold">User Analytics Dashboard</h5>
            <p class="text-muted">User statistics and charts will be implemented here.</p>
        </div>
    </div>
</div>
