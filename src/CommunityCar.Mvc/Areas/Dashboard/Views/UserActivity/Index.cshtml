@{
    ViewData["Title"] = "User Activity";
}

<div class="container-fluid py-4">
    <partial name="_UserActivityNav" />
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-0 text-gray-800">User Activity</h1>
            <p class="text-muted small">Monitor real-time user actions, logins, and profile changes.</p>
        </div>
    </div>

    <div class="card border-0 shadow-sm rounded-3">
        <div class="card-body p-5 text-center">
             <div class="mb-4">
                <i class="fas fa-walking fa-3x text-primary opacity-25"></i>
            </div>
            <h5 class="fw-bold">User Activity Log</h5>
            <p class="text-muted">A detailed feed of user interactions will be streamed here.</p>
        </div>
    </div>
</div>
