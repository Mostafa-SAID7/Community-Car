@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["Forgot Password Confirmation"];
    ViewData["HideSidebars"] = true;
    ViewData["HideFooter"] = true;
    var culture = System.Globalization.CultureInfo.CurrentUICulture.Name;
}

@section Styles {
    <link rel="stylesheet" href="~/css/pages/login.css" />
}

<div class="login-wrapper">
    <div class="login-container">
        <div class="login-header text-center">
            <div class="mb-4">
                <i class="fas fa-envelope-open-text fa-4x text-primary animate__animated animate__bounceIn"></i>
            </div>
            <h2>@Localizer["Check your email"]</h2>
            <p class="text-muted">@Localizer["Please check your email to reset your password."]</p>
        </div>

        <div class="text-center mt-4">
            <a href="/@culture/Login" class="btn btn-primary btn-lg w-100">@Localizer["Back to Login"]</a>
        </div>
    </div>
</div>
