@using CommunityCar.Domain.DTOs.Community
@model CommunityCar.Mvc.ViewModels.Qa.CreateQuestionViewModel
@{
    ViewData["Title"] = "Ask a Question";
}

@section RightSidebar {
    <partial name="Sidebars/_RightSidebar_QA" />
}

@section Styles {
    <style>
        .create-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 20px;
            padding: 40px;
            color: white;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
        }
        .create-card {
            border: none;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        }
        .form-control, .form-select {
            border-radius: 10px;
            border: 2px solid #e9ecef;
            padding: 12px 16px;
            transition: all 0.3s ease;
        }
        .form-control:focus, .form-select:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.15);
        }
        .btn-gradient {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            color: white;
            font-weight: 600;
            padding: 12px 32px;
            border-radius: 10px;
            transition: all 0.3s;
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
        }
        .btn-gradient:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
            color: white;
        }
        .tag-input {
            min-height: 50px;
        }
    </style>
}

<!-- Header -->
<div class="create-header">
    <div class="text-center">
        <h1 class="mb-2 fw-bold">
            <i class="fas fa-question-circle me-2"></i>Ask a Question
        </h1>
        <p class="mb-0 opacity-90">Get help from our community of car enthusiasts</p>
    </div>
</div>

<!-- Create Form -->
<div class="card create-card">
    <div class="card-body p-4 p-md-5">
        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger rounded-4"></div>
            
            <div class="mb-4">
                <label asp-for="Title" class="form-label fw-bold">
                    <i class="fas fa-heading me-2 text-primary"></i>Question Title
                </label>
                <input asp-for="Title" class="form-control form-control-lg" placeholder="What is your question? Be specific and clear." />
                <span asp-validation-for="Title" class="text-danger small"></span>
                <div class="form-text small text-muted mt-2">
                    <i class="fas fa-info-circle me-1"></i>
                    Write a clear, descriptive title that summarizes your question
                </div>
            </div>

            <div class="mb-4">
                <label asp-for="CategoryId" class="form-label fw-bold">
                    <i class="fas fa-folder me-2 text-primary"></i>Category
                </label>
                @if (Model?.Categories != null && Model.Categories.Any())
                {
                    <select asp-for="CategoryId" class="form-select form-select-lg" asp-items="Model.Categories">
                        <option value="">-- Select a Category --</option>
                    </select>
                }
                else
                {
                    <select name="CategoryId" class="form-select form-select-lg">
                        <option value="">-- Select a Category --</option>
                    </select>
                }
                <span asp-validation-for="CategoryId" class="text-danger small"></span>
            </div>

            <div class="mb-4">
                <label asp-for="Content" class="form-label fw-bold">
                    <i class="fas fa-align-left me-2 text-primary"></i>Question Details
                </label>
                <textarea asp-for="Content" class="form-control" rows="10" placeholder="Provide more context and details about your question...&#10;&#10;• What have you tried so far?&#10;• What specific problem are you facing?&#10;• Any error messages or symptoms?"></textarea>
                <span asp-validation-for="Content" class="text-danger small"></span>
                <div class="form-text small text-muted mt-2">
                    <i class="fas fa-info-circle me-1"></i>
                    Include all relevant details to help others understand and answer your question
                </div>
            </div>

            <div class="mb-4">
                <label class="form-label fw-bold">
                    <i class="fas fa-tags me-2 text-primary"></i>Tags
                </label>
                <input type="text" name="Tags" class="form-control tag-input" value="@(Model?.Tags ?? "")" placeholder="e.g. engine, maintenance, bmw, oil-change">
                <div class="form-text small text-muted mt-2">
                    <i class="fas fa-info-circle me-1"></i>
                    Add up to 5 tags separated by commas to help categorize your question
                </div>
            </div>

            <div class="alert alert-info rounded-4 mb-4">
                <h6 class="alert-heading mb-2">
                    <i class="fas fa-lightbulb me-2"></i>Tips for Getting Great Answers
                </h6>
                <ul class="mb-0 small">
                    <li>Be specific and clear in your question title</li>
                    <li>Provide all relevant details and context</li>
                    <li>Include any error messages or symptoms</li>
                    <li>Mention what you've already tried</li>
                    <li>Use appropriate tags to reach the right audience</li>
                </ul>
            </div>

            <div class="d-flex justify-content-between align-items-center pt-3 border-top">
                <a asp-action="Index" class="btn btn-light btn-lg px-4">
                    <i class="fas fa-times me-2"></i>Cancel
                </a>
                <button type="submit" class="btn btn-gradient btn-lg px-5">
                    <i class="fas fa-paper-plane me-2"></i>Post Your Question
                </button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <script>
        window.tagSuggestions = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(ViewBag.TagSuggestions ?? new List<TagDto>()));
    </script>
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        // Add character counter for title
        const titleInput = document.querySelector('input[name="Title"]');
        if (titleInput) {
            titleInput.addEventListener('input', function() {
                const length = this.value.length;
                const color = length > 100 ? 'text-danger' : length > 50 ? 'text-warning' : 'text-muted';
                let counter = this.parentElement.querySelector('.char-counter');
                if (!counter) {
                    counter = document.createElement('small');
                    counter.className = 'char-counter d-block mt-1';
                    this.parentElement.appendChild(counter);
                }
                counter.className = `char-counter d-block mt-1 ${color}`;
                counter.textContent = `${length} characters`;
            });
        }
    </script>
}
