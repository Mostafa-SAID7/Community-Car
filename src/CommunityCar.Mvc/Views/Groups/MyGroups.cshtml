@model List<CommunityCar.Web.ViewModels.Groups.GroupViewModel>
@{
    ViewData["Title"] = "My Groups";
    var currentPage = ViewBag.CurrentPage ?? 1;
    var totalPages = ViewBag.TotalPages ?? 1;
}

@section RightSidebar {
    <partial name="Sidebars/_RightSidebar_Social" />
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="fas fa-users me-2"></i>My Groups</h2>
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus me-2"></i>Create Group
        </a>
    </div>

    @if (Model.Any())
    {
        <div class="row g-4">
            @foreach (var group in Model)
            {
                <div class="col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm">
                        @if (!string.IsNullOrEmpty(group.ImageUrl))
                        {
                            <img src="@group.ImageUrl" class="card-img-top" alt="@group.Name" style="height: 200px; object-fit: cover;" />
                        }
                        else
                        {
                            <div class="card-img-top bg-secondary d-flex align-items-center justify-content-center" style="height: 200px;">
                                <i class="fas fa-users fa-4x text-white"></i>
                            </div>
                        }
                        <div class="card-body">
                            <h5 class="card-title">
                                <a asp-action="Details" asp-route-slug="@group.Slug" class="text-decoration-none">
                                    @group.Name
                                </a>
                                @if (group.IsAdmin)
                                {
                                    <span class="badge bg-primary ms-2">Admin</span>
                                }
                            </h5>
                            <p class="card-text text-muted">@group.Description</p>
                            <div class="d-flex justify-content-between align-items-center">
                                <small class="text-muted">
                                    <i class="fas fa-user me-1"></i>@group.MemberCount members
                                </small>
                            </div>
                        </div>
                        <div class="card-footer bg-white">
                            <a asp-action="Details" asp-route-slug="@group.Slug" class="btn btn-sm btn-outline-primary w-100">
                                View Group
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>

        @if (totalPages > 1)
        {
            <nav class="mt-4">
                <ul class="pagination justify-content-center">
                    <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                        <a class="page-link" asp-action="MyGroups" asp-route-page="@(currentPage - 1)">Previous</a>
                    </li>
                    @for (int i = 1; i <= totalPages; i++)
                    {
                        <li class="page-item @(i == currentPage ? "active" : "")">
                            <a class="page-link" asp-action="MyGroups" asp-route-page="@i">@i</a>
                        </li>
                    }
                    <li class="page-item @(currentPage == totalPages ? "disabled" : "")">
                        <a class="page-link" asp-action="MyGroups" asp-route-page="@(currentPage + 1)">Next</a>
                    </li>
                </ul>
            </nav>
        }
    }
    else
    {
        <div class="card">
            <div class="card-body text-center py-5">
                <i class="fas fa-users fa-4x text-muted mb-3"></i>
                <h5 class="text-muted">You haven't joined any groups yet</h5>
                <p class="text-muted">Join existing groups or create your own!</p>
                <div class="d-flex gap-2 justify-content-center">
                    <a asp-action="Index" class="btn btn-outline-primary">
                        <i class="fas fa-search me-2"></i>Browse Groups
                    </a>
                    <a asp-action="Create" class="btn btn-primary">
                        <i class="fas fa-plus me-2"></i>Create Group
                    </a>
                </div>
            </div>
        </div>
    }
</div>
