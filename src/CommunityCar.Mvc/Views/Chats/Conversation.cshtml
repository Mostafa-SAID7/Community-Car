@model List<CommunityCar.Domain.DTOs.Communications.ChatMessageDto>
@{
    ViewData["Title"] = "Chat";
    var otherUserId = ViewBag.OtherUserId as Guid?;
    var conversations = ViewBag.Conversations as List<CommunityCar.Domain.DTOs.Communications.ChatConversationDto>;
    var otherUser = conversations?.FirstOrDefault(c => c.UserId == otherUserId);
    var currentUserId = User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value;
}

<div class="container py-3">
    <div class="card border-0 shadow-lg rounded-4 overflow-hidden chat-container">
        <div class="row g-0 h-100">
            <!-- Sidebar -->
            <div class="col-md-4 col-lg-3 border-end d-flex flex-column h-100 bg-white">
                <div class="p-4 border-bottom d-flex justify-content-between align-items-center">
                    <h5 class="mb-0 fw-bold">Chats</h5>
                    <a asp-action="Index" class="btn btn-light btn-sm rounded-circle shadow-sm">
                        <i class="fas fa-search"></i>
                    </a>
                </div>
                <div class="conversations-list p-2">
                    @if (conversations != null)
                    {
                        foreach (var conv in conversations)
                        {
                            <a href="@Url.Action("Conversation", new { id = conv.UserId })" 
                               class="d-block text-decoration-none conv-item p-3 @(conv.UserId == otherUserId ? "active" : "")"
                               data-user-id="@conv.UserId">
                                <div class="d-flex align-items-center">
                                    <div class="position-relative me-3">
                                        @if (!string.IsNullOrEmpty(conv.ProfilePicture))
                                        {
                                            <img src="@conv.ProfilePicture" class="rounded-circle user-avatar-sm">
                                        }
                                        else
                                        {
                                            <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center fw-bold user-avatar-sm">
                                                @conv.UserName.Substring(0, 1).ToUpper()
                                            </div>
                                        }
                                        @if (conv.IsOnline)
                                        {
                                            <span class="position-absolute bottom-0 end-0 bg-success border border-white rounded-circle" style="width:10px; height:10px;"></span>
                                        }
                                    </div>
                                    <div class="flex-grow-1 text-truncate">
                                        <div class="fw-bold text-dark small">@conv.UserName</div>
                                        <div class="text-muted small text-truncate">@(conv.LastMessage ?? "No messages")</div>
                                    </div>
                                    @if (conv.UnreadCount > 0)
                                    {
                                        <span class="badge bg-danger rounded-pill">@conv.UnreadCount</span>
                                    }
                                </div>
                            </a>
                        }
                    }
                </div>
            </div>

            <!-- Chat Area -->
            <div class="col-md-8 col-lg-9 d-flex flex-column h-100 bg-white">
                @if (otherUser != null)
                {
                    <!-- Chat Header -->
                    <div class="p-3 border-bottom d-flex align-items-center justify-content-between bg-white">
                        <div class="d-flex align-items-center">
                            <div class="position-relative me-3">
                                @if (!string.IsNullOrEmpty(otherUser.ProfilePicture))
                                {
                                    <img src="@otherUser.ProfilePicture" class="rounded-circle user-avatar-sm">
                                }
                                else
                                {
                                    <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center fw-bold user-avatar-sm">
                                        @otherUser.UserName.Substring(0, 1).ToUpper()
                                    </div>
                                }
                                <div id="statusDot" class="position-absolute bottom-0 end-0 border border-white rounded-circle @(otherUser.IsOnline ? "bg-success" : "bg-secondary")" style="width:12px; height:12px;"></div>
                            </div>
                            <div>
                                <h6 class="mb-0 fw-bold">@otherUser.UserName</h6>
                                <small id="statusText" class="text-muted">@(otherUser.IsOnline ? "Online" : "Offline")</small>
                            </div>
                        </div>
                        <div class="dropdown">
                            <button class="btn btn-light rounded-circle btn-sm shadow-sm" data-bs-toggle="dropdown">
                                <i class="fas fa-ellipsis-v"></i>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end border-0 shadow-sm">
                                <li><a class="dropdown-item" href="@Url.Action("Index", "Profiles", new { area = "Identity", id = otherUser.UserId })">View Profile</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-danger" href="#">Block User</a></li>
                            </ul>
                        </div>
                    </div>

                    <!-- Messages -->
                    <div class="chat-messages-area p-4" id="chatMessages">
                        @foreach (var message in Model.OrderBy(m => m.CreatedAt))
                        {
                            var isSent = message.SenderId.ToString() == currentUserId;
                            <div class="d-flex flex-column @(isSent ? "align-items-end" : "align-items-start") mb-2">
                                <div class="message-bubble @(isSent ? "message-sent" : "message-received")">
                                    <p class="mb-0">@message.Content</p>
                                </div>
                                <small class="text-muted" style="font-size: 0.7rem;">
                                    @message.CreatedAt.ToString("HH:mm")
                                    @if(isSent) {
                                        <i class="fas @(message.IsRead ? "fa-check-double text-primary" : "fa-check") ms-1"></i>
                                    }
                                </small>
                            </div>
                        }
                    </div>

                    <!-- Input Area -->
                    <div class="p-3 border-top bg-white">
                        <div id="typingIndicator" class="typing-indicator mb-1 px-2"></div>
                        <form id="sendMessageForm" class="d-flex gap-2 align-items-center">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="ReceiverId" value="@otherUserId" />
                            <div class="flex-grow-1 position-relative">
                                <input type="text" name="Content" id="messageInput" class="form-control rounded-pill py-2 border-0 bg-light shadow-none" 
                                       placeholder="Type a message..." required maxlength="2000" autocomplete="off">
                            </div>
                            <button type="submit" class="btn btn-primary rounded-circle shadow-sm flex-shrink-0" style="width:40px; height:40px;">
                                <i class="fas fa-paper-plane"></i>
                            </button>
                        </form>
                    </div>
                }
                else
                {
                    <div class="d-flex flex-column align-items-center justify-content-center h-100 text-center p-5">
                        <i class="fas fa-comments fa-5x opacity-10 mb-4"></i>
                        <h4 class="fw-bold text-muted">Select a conversation</h4>
                        <p class="text-muted">Pick a contact from the list to start chatting</p>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            const receiverId = '@otherUserId';
            const currentUserId = '@currentUserId';
            const chatMessages = document.getElementById('chatMessages');
            let typingTimeout;

            if (!receiverId) return;

            // Initialize SignalR
            const connection = new signalR.HubConnectionBuilder()
                .withUrl("/chatHub")
                .withAutomaticReconnect()
                .build();

            // Receive message
            connection.on("ReceiveMessage", function(senderId, content, timestamp) {
                if (senderId === receiverId) {
                    appendMessage(content, timestamp, false);
                    markAsRead();
                } else {
                    // Update unread count in sidebar or show toast
                    const badge = $('[data-user-id="' + senderId + '"] .badge');
                    if (badge.length) {
                        const count = parseInt(badge.text() || "0") + 1;
                        badge.text(count).show();
                    }
                }
            });

            // Message confirmation
            connection.on("MessageSent", function(receiverId, content, timestamp) {
                // We handle this via AJAX success for better UX
            });

            // Status updates
            connection.on("UserOnline", function(userId) {
                updateUserStatus(userId, true);
            });
            connection.on("UserOffline", function(userId) {
                updateUserStatus(userId, false);
            });

            // Typing indicators
            connection.on("UserTyping", function(userId) {
                if (userId === receiverId) {
                    $('#typingIndicator').text('@(otherUser?.UserName) is typing...');
                }
            });
            connection.on("UserStoppedTyping", function(userId) {
                if (userId === receiverId) {
                    $('#typingIndicator').text('');
                }
            });

            connection.start()
                .then(() => console.log("ChatHub Connected"))
                .catch(err => console.error(err));

            // UI Helpers
            function scrollToBottom() {
                chatMessages.scrollTop = chatMessages.scrollHeight;
            }
            scrollToBottom();

            function appendMessage(content, timestamp, isSent) {
                const date = new Date(timestamp);
                const timeStr = date.getHours().toString().padStart(2, '0') + ':' + date.getMinutes().toString().padStart(2, '0');
                
                const html = `
                    <div class="d-flex flex-column ${isSent ? 'align-items-end' : 'align-items-start'} mb-2">
                        <div class="message-bubble ${isSent ? 'message-sent' : 'message-received'}">
                            <p class="mb-0">${content}</p>
                        </div>
                        <small class="text-muted" style="font-size: 0.7rem;">
                            ${timeStr} ${isSent ? '<i class="fas fa-check ms-1"></i>' : ''}
                        </small>
                    </div>`;
                
                $(chatMessages).append(html);
                scrollToBottom();
            }

            function updateUserStatus(userId, isOnline) {
                const indicator = $('[data-user-id="' + userId + '"] .bg-success, [data-user-id="' + userId + '"] .bg-secondary');
                if (isOnline) {
                    indicator.removeClass('bg-secondary').addClass('bg-success');
                } else {
                    indicator.removeClass('bg-success').addClass('bg-secondary');
                }

                if (userId === receiverId) {
                    $('#statusDot').removeClass('bg-secondary bg-success').addClass(isOnline ? 'bg-success' : 'bg-secondary');
                    $('#statusText').text(isOnline ? 'Online' : 'Offline');
                }
            }

            function markAsRead() {
                const url = CultureHelper.addCultureToUrl('/Chats/MarkConversationAsRead/' + receiverId);
                $.post(url, {
                    __RequestVerificationToken: $('input[name="__RequestVerificationToken"]').val()
                });
            }

            // Forms & Inputs
            $('#sendMessageForm').on('submit', function(e) {
                e.preventDefault();
                const content = $('#messageInput').val().trim();
                if (!content) return;

                const formData = $(this).serialize();
                const url = CultureHelper.addCultureToUrl('/Chats/SendMessage');
                $.post(url, formData, function(response) {
                    if (response.success) {
                        appendMessage(content, new Date(), true);
                        $('#messageInput').val('');
                        if (connection.state === signalR.HubConnectionState.Connected) {
                            connection.invoke("SendMessage", receiverId, content);
                        }
                    }
                });
            });

            $('#messageInput').on('input', function() {
                if (connection.state === signalR.HubConnectionState.Connected) {
                    connection.invoke("Typing", receiverId);
                    clearTimeout(typingTimeout);
                    typingTimeout = setTimeout(() => {
                        connection.invoke("StopTyping", receiverId);
                    }, 1000);
                }
            });

            markAsRead();
        });
    </script>
}
