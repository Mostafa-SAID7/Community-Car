@model CommunityCar.Mvc.ViewModels.Feed.FeedViewModel
@{
    ViewData["Title"] = "Trending Content";
}

<div class="container-fluid py-4">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h2 class="mb-0">
                        <i class="fas fa-chart-line text-primary"></i> Trending Content
                    </h2>
                    <p class="text-muted mb-0">Most engaging content in the last 24 hours</p>
                </div>
                <a asp-action="Index" class="btn btn-outline-secondary">
                    <i class="fas fa-arrow-left"></i> Back to Feed
                </a>
            </div>
        </div>
    </div>
    
    <!-- Use the same feed layout -->
    <partial name="Index" model="Model" />
</div>
