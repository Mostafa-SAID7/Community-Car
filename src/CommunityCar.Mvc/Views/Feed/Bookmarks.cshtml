@model CommunityCar.Mvc.ViewModels.Feed.FeedViewModel
@{
    ViewData["Title"] = "Bookmarked Content";
}

@section RightSidebar {
    <partial name="Sidebars/_RightSidebar_Feed" />
}

@section Styles {
    <link rel="stylesheet" href="~/css/feed.css" />
}

@{
    ViewData["HeaderTitle"] = "Bookmarks";
    ViewData["HeaderDescription"] = "Content you've saved for later";
    ViewData["HeaderIcon"] = "fas fa-bookmark";
}

<partial name="_InnerPageHeader" />

<!-- Filters -->
<partial name="_FeedFilters" model="Model.Filters" />

<!-- Feed Items -->
@if (Model.Items.Any())
{
    <div class="feed-items">
        @foreach (var item in Model.Items)
        {
            <partial name="_FeedItem" model="item" />
        }
    </div>

    <!-- Pagination -->
    @if (Model.TotalPages > 1)
    {
        <nav aria-label="Feed pagination">
            <ul class="pagination justify-content-center">
                <li class="page-item @(!Model.HasPreviousPage ? "disabled" : "")">
                    <a class="page-link" 
                       asp-action="Bookmarks" 
                       asp-route-page="@(Model.CurrentPage - 1)">
                        <i class="fas fa-chevron-left"></i> Previous
                    </a>
                </li>

                @for (int i = Math.Max(1, Model.CurrentPage - 2); i <= Math.Min(Model.TotalPages, Model.CurrentPage + 2); i++)
                {
                    <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                        <a class="page-link" 
                           asp-action="Bookmarks" 
                           asp-route-page="@i">
                            @i
                        </a>
                    </li>
                }

                <li class="page-item @(!Model.HasNextPage ? "disabled" : "")">
                    <a class="page-link" 
                       asp-action="Bookmarks" 
                       asp-route-page="@(Model.CurrentPage + 1)">
                        Next <i class="fas fa-chevron-right"></i>
                    </a>
                </li>
            </ul>
        </nav>
    }
}
else
{
    <div class="card">
        <div class="card-body text-center py-5">
            <i class="fas fa-bookmark fa-3x text-muted mb-3"></i>
            <h5 class="text-muted">No bookmarked content</h5>
            <p class="text-muted">Save content to your bookmarks to access it later.</p>
            <a asp-action="Index" class="btn btn-primary">
                <i class="fas fa-stream"></i> Browse Feed
            </a>
        </div>
    </div>
}

@section Scripts {
    <script src="~/js/feed.js"></script>
}
