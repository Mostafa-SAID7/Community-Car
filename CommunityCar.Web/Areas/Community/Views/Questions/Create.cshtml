@model CommunityCar.Web.Areas.Community.ViewModels.qa.CreateQuestionViewModel
@{
    ViewData["Title"] = "Ask a Question";
    var categories = ViewBag.Categories as List<CommunityCar.Domain.DTOs.Community.CategoryDto> ?? new();
    var popularTags = ViewBag.PopularTags as List<CommunityCar.Domain.DTOs.Community.TagDto> ?? new();
}

<div class="container" style="max-width: 900px; margin: 40px auto;">
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">Ask a Question</h1>
            <p class="text-secondary">Get help from the community</p>
        </div>

        <div class="card-body">
            <form asp-action="Crea